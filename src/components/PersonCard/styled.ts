import { themeParams } from '@theme';

import styled from 'styled-components';

export const CardInfo = styled.div`
  position: absolute;
  left: ${themeParams.spaces[4]};
  bottom: ${themeParams.spaces[3]};
`;
